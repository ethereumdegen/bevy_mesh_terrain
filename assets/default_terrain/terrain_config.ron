// terrain_config.ron
TerrainConfig(
    terrain_dimensions: (1024.0, 1024.0), // Vec2(x, y)
    chunk_rows: 16,
    render_distance: 2000.0,
    lod_distance: 1000.0,
    lod_level_offset: 0,
    height_scale: 0.004,
    attach_collision_data: true,
    // Assuming the addition of fields for paths to the height and splat folders
    height_folder_path: "diffuse", // String
    splat_folder_path: "splat", // String
     diffuse_folder_path: "height" // String
     
)
